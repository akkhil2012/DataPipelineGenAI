[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "datapipeline-genai"
version = "0.1.0"
description = "Data pipeline for customer support analytics enhanced with heuristic LLM summarization"
readme = "README.md"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
datapipeline-genai = "datapipeline_genai.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
